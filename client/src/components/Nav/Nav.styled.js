import styled from "styled-components"

export const StyledNav = styled.nav`
  position: relative;

  ul {
    display: flex;
    gap: 1rem;
    align-items: center;
    list-style: none;
  }

  button {
    cursor: pointer;
    background-color: transparent;
    border: none;
  }
`
